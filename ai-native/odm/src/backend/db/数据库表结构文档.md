# 数据库表结构文档

**生成时间**: 2025-12-12 09:05:41

**数据库**: lifetree

---

## odm_bom_mapping

### 表说明

（待补充表的具体用途说明）

### 字段列表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | `bigint(20)` | 主键, 非空, 自增 | （待补充字段说明） |
| `creator` | `varchar(100)` | - | （待补充字段说明） |
| `is_valid` | `tinyint(1)` | 索引 | （待补充字段说明） |
| `create_time` | `datetime` | 索引 | （待补充字段说明） |
| `updater` | `varchar(100)` | - | （待补充字段说明） |
| `update_time` | `datetime` | - | （待补充字段说明） |
| `product_model` | `varchar(100)` | 索引 | （待补充字段说明） |
| `latest_bom` | `varchar(100)` | - | （待补充字段说明） |
| `product_series` | `varchar(100)` | - | （待补充字段说明） |
| `device_type` | `varchar(100)` | - | （待补充字段说明） |

### 建表语句

```sql
CREATE TABLE `odm_bom_mapping` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `creator` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人',
  `is_valid` tinyint(1) DEFAULT '1' COMMENT '是否有效 (1-有效, 0-无效)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updater` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '更新人',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `product_model` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '产品型号',
  `latest_bom` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '最新BOM',
  `product_series` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '产品系列',
  `device_type` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '设备类型',
  PRIMARY KEY (`id`),
  KEY `idx_product_model` (`product_model`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_is_valid` (`is_valid`)
) ENGINE=InnoDB AUTO_INCREMENT=11 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='型号映射表'
```


---

## odm_bom_record

### 表说明

（待补充表的具体用途说明）

### 字段列表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | `bigint(20)` | 主键, 非空, 自增 | （待补充字段说明） |
| `creator` | `varchar(100)` | - | （待补充字段说明） |
| `is_valid` | `tinyint(1)` | 索引 | （待补充字段说明） |
| `create_time` | `datetime` | 索引 | （待补充字段说明） |
| `updater` | `varchar(100)` | - | （待补充字段说明） |
| `update_time` | `datetime` | - | （待补充字段说明） |
| `odm_bom` | `varchar(100)` | 索引 | （待补充字段说明） |
| `odm_model` | `varchar(100)` | 索引 | （待补充字段说明） |
| `structure_requirement` | `json` | - | （待补充字段说明） |
| `change_history` | `json` | - | （待补充字段说明） |
| `requirement_work_order_no` | `json` | - | （待补充字段说明） |
| `bom_name` | `varchar(200)` | 索引 | （待补充字段说明） |
| `base_bom_part_number` | `varchar(100)` | 索引 | （待补充字段说明） |
| `base_bom_parent_part_number` | `varchar(100)` | - | （待补充字段说明） |
| `base_bom_position_number` | `varchar(50)` | - | （待补充字段说明） |
| `base_bom_quantity` | `decimal(10,2)` | - | （待补充字段说明） |
| `base_bom_workstation` | `varchar(100)` | - | （待补充字段说明） |
| `base_bom_description` | `text` | - | （待补充字段说明） |
| `change_bom_part_number` | `varchar(100)` | 索引 | （待补充字段说明） |
| `change_bom_parent_part_number` | `varchar(100)` | - | （待补充字段说明） |
| `change_bom_position_number` | `varchar(50)` | - | （待补充字段说明） |
| `change_bom_quantity` | `decimal(10,2)` | - | （待补充字段说明） |
| `change_bom_workstation` | `varchar(100)` | - | （待补充字段说明） |
| `change_operation` | `varchar(50)` | - | （待补充字段说明） |

### 建表语句

```sql
CREATE TABLE `odm_bom_record` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `creator` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人',
  `is_valid` tinyint(1) DEFAULT '1' COMMENT '是否有效 (1-有效, 0-无效)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updater` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '更新人',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `odm_bom` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'ODM BOM',
  `odm_model` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'ODM型号',
  `structure_requirement` json DEFAULT NULL COMMENT '结构诉求',
  `change_history` json DEFAULT NULL COMMENT '变更历史',
  `requirement_work_order_no` json DEFAULT NULL COMMENT '需求工单号',
  `bom_name` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'BOM名称',
  `base_bom_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM料号',
  `base_bom_parent_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM上阶料号',
  `base_bom_position_number` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM位置号',
  `base_bom_quantity` decimal(10,2) DEFAULT NULL COMMENT '基础BOM数量',
  `base_bom_workstation` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM工位',
  `base_bom_description` text COLLATE utf8mb4_unicode_ci COMMENT '基础BOM说明',
  `change_bom_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM料号',
  `change_bom_parent_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM上阶料号',
  `change_bom_position_number` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM位置号',
  `change_bom_quantity` decimal(10,2) DEFAULT NULL COMMENT '变更BOM数量',
  `change_bom_workstation` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM工位',
  `change_operation` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更操作 (如: 新增/删除/修改)',
  PRIMARY KEY (`id`),
  KEY `idx_odm_bom` (`odm_bom`),
  KEY `idx_odm_model` (`odm_model`),
  KEY `idx_base_bom_part_number` (`base_bom_part_number`),
  KEY `idx_change_bom_part_number` (`change_bom_part_number`),
  KEY `idx_bom_name` (`bom_name`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_is_valid` (`is_valid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='结构设计BOM记录表'
```


---

## odm_bom_record_log

### 表说明

（待补充表的具体用途说明）

### 字段列表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | `bigint(20)` | 主键, 非空, 自增 | （待补充字段说明） |
| `creator` | `varchar(100)` | - | （待补充字段说明） |
| `is_valid` | `tinyint(1)` | - | （待补充字段说明） |
| `origin_id` | `bigint(20)` | 非空 | （待补充字段说明） |
| `odm_bom` | `varchar(100)` | 索引 | （待补充字段说明） |
| `odm_model` | `varchar(100)` | 索引 | （待补充字段说明） |
| `structure_requirement` | `json` | - | （待补充字段说明） |
| `requirement_work_order_no` | `json` | - | （待补充字段说明） |
| `bom_name` | `varchar(200)` | 索引 | （待补充字段说明） |
| `operator_user_id` | `varchar(100)` | 索引 | （待补充字段说明） |
| `operation_time` | `datetime` | 索引 | （待补充字段说明） |
| `operation_desc` | `varchar(500)` | 索引 | （待补充字段说明） |
| `base_bom_part_number` | `varchar(100)` | 索引 | （待补充字段说明） |
| `base_bom_parent_part_number` | `varchar(100)` | - | （待补充字段说明） |
| `base_bom_position_number` | `varchar(50)` | - | （待补充字段说明） |
| `base_bom_quantity` | `decimal(10,2)` | - | （待补充字段说明） |
| `base_bom_workstation` | `varchar(100)` | - | （待补充字段说明） |
| `base_bom_description` | `text` | - | （待补充字段说明） |
| `change_bom_part_number` | `varchar(100)` | 索引 | （待补充字段说明） |
| `change_bom_parent_part_number` | `varchar(100)` | - | （待补充字段说明） |
| `change_bom_position_number` | `varchar(50)` | - | （待补充字段说明） |
| `change_bom_quantity` | `decimal(10,2)` | - | （待补充字段说明） |
| `change_bom_workstation` | `varchar(100)` | - | （待补充字段说明） |
| `change_operation` | `varchar(50)` | - | （待补充字段说明） |

### 建表语句

```sql
CREATE TABLE `odm_bom_record_log` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `creator` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人',
  `is_valid` tinyint(1) DEFAULT '1' COMMENT '是否有效 (1-有效, 0-无效)',
  `origin_id` bigint(20) NOT NULL COMMENT '操作主表的主键ID (关联 odm_bom_record.id)',
  `odm_bom` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'ODM BOM',
  `odm_model` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'ODM型号',
  `structure_requirement` json DEFAULT NULL COMMENT '结构诉求',
  `requirement_work_order_no` json DEFAULT NULL COMMENT '需求工单号',
  `bom_name` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'BOM名称',
  `operator_user_id` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '操作用户ID',
  `operation_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  `operation_desc` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '操作描述',
  `base_bom_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM料号',
  `base_bom_parent_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM上阶料号',
  `base_bom_position_number` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM位置号',
  `base_bom_quantity` decimal(10,2) DEFAULT NULL COMMENT '基础BOM数量',
  `base_bom_workstation` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '基础BOM工位',
  `base_bom_description` text COLLATE utf8mb4_unicode_ci COMMENT '基础BOM说明',
  `change_bom_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM料号',
  `change_bom_parent_part_number` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM上阶料号',
  `change_bom_position_number` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM位置号',
  `change_bom_quantity` decimal(10,2) DEFAULT NULL COMMENT '变更BOM数量',
  `change_bom_workstation` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更BOM工位',
  `change_operation` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更操作 (如: 新增/删除/修改)',
  PRIMARY KEY (`id`),
  KEY `idx_odm_bom` (`odm_bom`),
  KEY `idx_odm_model` (`odm_model`),
  KEY `idx_base_bom_part_number` (`base_bom_part_number`),
  KEY `idx_change_bom_part_number` (`change_bom_part_number`),
  KEY `idx_bom_name` (`bom_name`),
  KEY `idx_operator_user_id` (`operator_user_id`),
  KEY `idx_operation_time` (`operation_time`),
  KEY `idx_operation_desc` (`operation_desc`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='结构设计BOM操作记录表'
```


---

## odm_company_info

### 表说明

（待补充表的具体用途说明）

### 字段列表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | `bigint(20)` | 主键, 非空, 自增 | （待补充字段说明） |
| `creator` | `varchar(100)` | - | （待补充字段说明） |
| `is_valid` | `tinyint(1)` | 索引 | （待补充字段说明） |
| `create_time` | `datetime` | 索引 | （待补充字段说明） |
| `updater` | `varchar(100)` | - | （待补充字段说明） |
| `update_time` | `datetime` | - | （待补充字段说明） |
| `odm_name` | `varchar(50)` | 非空 | （待补充字段说明） |
| `odm_code` | `varchar(50)` | - | （待补充字段说明） |
| `company_name` | `varchar(200)` | 索引 | （待补充字段说明） |
| `company_address` | `varchar(500)` | - | （待补充字段说明） |
| `company_phone` | `varchar(50)` | - | （待补充字段说明） |
| `company_fax` | `varchar(50)` | - | （待补充字段说明） |
| `company_website` | `varchar(200)` | - | （待补充字段说明） |
| `service_400_phone` | `varchar(50)` | - | （待补充字段说明） |
| `service_email` | `varchar(100)` | - | （待补充字段说明） |
| `service_qrcode` | `varchar(500)` | - | （待补充字段说明） |
| `product_model_rule` | `varchar(100)` | - | （待补充字段说明） |
| `machine_serial_rule` | `varchar(100)` | - | （待补充字段说明） |
| `cover_logo_vector` | `varchar(500)` | - | （待补充字段说明） |
| `cover_color_plate` | `varchar(500)` | - | （待补充字段说明） |
| `cover_color_card` | `varchar(500)` | - | （待补充字段说明） |
| `electrical_label` | `varchar(500)` | - | （待补充字段说明） |
| `carton` | `varchar(500)` | - | （待补充字段说明） |
| `carton_label` | `varchar(500)` | - | （待补充字段说明） |
| `cqc_certificate` | `varchar(500)` | - | （待补充字段说明） |
| `certificate` | `varchar(500)` | - | （待补充字段说明） |
| `warranty_card` | `varchar(500)` | - | （待补充字段说明） |
| `quick_install_guide` | `varchar(500)` | - | （待补充字段说明） |
| `inspection_report` | `varchar(500)` | - | （待补充字段说明） |
| `communication_stick` | `varchar(500)` | - | （待补充字段说明） |
| `other_packaging_requirements` | `text` | - | （待补充字段说明） |
| `communication_protocol` | `varchar(200)` | - | （待补充字段说明） |
| `app` | `varchar(200)` | - | （待补充字段说明） |
| `cloud` | `varchar(200)` | - | （待补充字段说明） |
| `sim_card` | `varchar(200)` | - | （待补充字段说明） |
| `empty_shell_machine` | `varchar(200)` | - | （待补充字段说明） |
| `sample_machine` | `varchar(200)` | - | （待补充字段说明） |

### 建表语句

```sql
CREATE TABLE `odm_company_info` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `creator` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人',
  `is_valid` tinyint(1) DEFAULT '1' COMMENT '是否有效 (1-有效, 0-无效)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updater` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '更新人',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `odm_name` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '简写',
  `odm_code` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '客户ODM代码',
  `company_name` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司全称',
  `company_address` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司地址',
  `company_phone` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司联系电话',
  `company_fax` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司传真',
  `company_website` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司官网',
  `service_400_phone` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '服务400电话',
  `service_email` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '服务邮箱',
  `service_qrcode` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '服务公众号二维码 (存储路径或URL)',
  `product_model_rule` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '产品型号规则',
  `machine_serial_rule` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '机器序列号规则',
  `cover_logo_vector` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '上盖logo矢量图 (存储路径)',
  `cover_color_plate` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '上盖色板 (存储路径)',
  `cover_color_card` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '上盖色卡 (存储路径)',
  `electrical_label` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '电气标签 (存储路径)',
  `carton` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '纸箱 (存储路径)',
  `carton_label` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '纸箱标签 (存储路径)',
  `cqc_certificate` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'CQC副证 (存储路径)',
  `certificate` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '合格证 (存储路径)',
  `warranty_card` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '质保卡 (存储路径)',
  `quick_install_guide` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '机器快装 (存储路径)',
  `inspection_report` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '出厂检验报告 (存储路径)',
  `communication_stick` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '通讯棒 (存储路径)',
  `other_packaging_requirements` text COLLATE utf8mb4_unicode_ci COMMENT '其他附件包装要求',
  `communication_protocol` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '通讯协议',
  `app` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'APP',
  `cloud` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'Cloud',
  `sim_card` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'SIM卡',
  `empty_shell_machine` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '空壳机',
  `sample_machine` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '样机',
  PRIMARY KEY (`id`),
  KEY `idx_company_name` (`company_name`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_is_valid` (`is_valid`)
) ENGINE=InnoDB AUTO_INCREMENT=4 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='ODM信息管理表（产品定制化物料和项目变更输入信息）'
```


---

## odm_request_order

### 表说明

（待补充表的具体用途说明）

### 字段列表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | `bigint(20)` | 主键, 非空, 自增 | （待补充字段说明） |
| `creator` | `varchar(100)` | - | （待补充字段说明） |
| `is_valid` | `tinyint(1)` | 索引 | （待补充字段说明） |
| `create_time` | `datetime` | 索引 | （待补充字段说明） |
| `updater` | `varchar(100)` | - | （待补充字段说明） |
| `update_time` | `datetime` | - | （待补充字段说明） |
| `work_order_no` | `varchar(100)` | 唯一 | （待补充字段说明） |
| `related_work_order_no` | `varchar(100)` | 索引 | （待补充字段说明） |
| `odm_code` | `varchar(200)` | 索引 | （待补充字段说明） |
| `selected_model_list` | `json` | - | （待补充字段说明） |
| `structure_requirement` | `json` | - | （待补充字段说明） |
| `document_requirement` | `json` | - | （待补充字段说明） |
| `company_name` | `varchar(200)` | 索引 | （待补充字段说明） |
| `company_address` | `varchar(500)` | - | （待补充字段说明） |
| `company_phone` | `varchar(50)` | - | （待补充字段说明） |
| `company_fax` | `varchar(50)` | - | （待补充字段说明） |
| `company_website` | `varchar(200)` | - | （待补充字段说明） |
| `service_400_phone` | `varchar(50)` | - | （待补充字段说明） |
| `service_email` | `varchar(100)` | - | （待补充字段说明） |
| `service_qrcode` | `varchar(500)` | - | （待补充字段说明） |
| `product_model_rule` | `varchar(100)` | - | （待补充字段说明） |
| `machine_serial_rule` | `varchar(100)` | - | （待补充字段说明） |
| `cover_logo_vector` | `varchar(500)` | - | （待补充字段说明） |
| `cover_color_plate` | `varchar(500)` | - | （待补充字段说明） |
| `cover_color_card` | `varchar(500)` | - | （待补充字段说明） |
| `electrical_label` | `varchar(500)` | - | （待补充字段说明） |
| `carton` | `varchar(500)` | - | （待补充字段说明） |
| `carton_label` | `varchar(500)` | - | （待补充字段说明） |
| `cqc_certificate` | `varchar(500)` | - | （待补充字段说明） |
| `certificate` | `varchar(500)` | - | （待补充字段说明） |
| `warranty_card` | `varchar(500)` | - | （待补充字段说明） |
| `quick_install_guide` | `varchar(500)` | - | （待补充字段说明） |
| `inspection_report` | `varchar(500)` | - | （待补充字段说明） |
| `communication_stick` | `varchar(500)` | - | （待补充字段说明） |
| `other_packaging_requirements` | `text` | - | （待补充字段说明） |
| `communication_protocol` | `varchar(200)` | - | （待补充字段说明） |
| `app` | `varchar(200)` | - | （待补充字段说明） |
| `cloud` | `varchar(200)` | - | （待补充字段说明） |
| `sim_card` | `varchar(200)` | - | （待补充字段说明） |
| `empty_shell_machine` | `varchar(200)` | - | （待补充字段说明） |
| `sample_machine` | `varchar(200)` | - | （待补充字段说明） |

### 建表语句

```sql
CREATE TABLE `odm_request_order` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `creator` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人',
  `is_valid` tinyint(1) DEFAULT '1' COMMENT '是否有效 (1-有效, 0-无效)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updater` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '更新人',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `work_order_no` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '工单号',
  `related_work_order_no` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '关联工单号',
  `odm_code` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '客户ODM代码',
  `selected_model_list` json DEFAULT NULL COMMENT '选择系列号',
  `structure_requirement` json DEFAULT NULL COMMENT '结构变更描述',
  `document_requirement` json DEFAULT NULL COMMENT '文档变更描述',
  `company_name` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司全称',
  `company_address` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司地址',
  `company_phone` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司联系电话',
  `company_fax` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司传真',
  `company_website` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '公司官网',
  `service_400_phone` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '服务400电话',
  `service_email` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '服务邮箱',
  `service_qrcode` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '服务公众号二维码 (存储路径或URL)',
  `product_model_rule` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '产品型号规则',
  `machine_serial_rule` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '机器序列号规则',
  `cover_logo_vector` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '上盖logo矢量图 (存储路径)',
  `cover_color_plate` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '上盖色板 (存储路径)',
  `cover_color_card` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '上盖色卡 (存储路径)',
  `electrical_label` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '电气标签 (存储路径)',
  `carton` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '纸箱 (存储路径)',
  `carton_label` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '纸箱标签 (存储路径)',
  `cqc_certificate` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'CQC副证 (存储路径)',
  `certificate` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '合格证 (存储路径)',
  `warranty_card` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '质保卡 (存储路径)',
  `quick_install_guide` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '机器快装 (存储路径)',
  `inspection_report` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '出厂检验报告 (存储路径)',
  `communication_stick` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '通讯棒 (存储路径)',
  `other_packaging_requirements` text COLLATE utf8mb4_unicode_ci,
  `communication_protocol` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '通讯协议',
  `app` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'APP',
  `cloud` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'Cloud',
  `sim_card` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'SIM卡',
  `empty_shell_machine` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '空壳机',
  `sample_machine` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '样机',
  PRIMARY KEY (`id`),
  UNIQUE KEY `work_order_no` (`work_order_no`),
  KEY `idx_work_order_no` (`work_order_no`),
  KEY `idx_related_work_order_no` (`related_work_order_no`),
  KEY `idx_customer_name` (`odm_code`),
  KEY `idx_company_name` (`company_name`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_is_valid` (`is_valid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='ODM需求工单管理表'
```


---

## odm_request_order_log

### 表说明

（待补充表的具体用途说明）

### 字段列表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | `bigint(20)` | 主键, 非空, 自增 | （待补充字段说明） |
| `creator` | `varchar(100)` | - | （待补充字段说明） |
| `is_valid` | `tinyint(1)` | - | （待补充字段说明） |
| `origin_id` | `bigint(20)` | 索引, 非空 | （待补充字段说明） |
| `related_work_order_no` | `varchar(100)` | 索引 | （待补充字段说明） |
| `operator_user_id` | `varchar(100)` | 索引 | （待补充字段说明） |
| `operation_time` | `datetime` | 索引 | （待补充字段说明） |
| `operation_desc` | `varchar(500)` | 索引 | （待补充字段说明） |
| `changed_field_name` | `varchar(200)` | 索引 | （待补充字段说明） |
| `changed_value` | `varchar(500)` | - | （待补充字段说明） |

### 建表语句

```sql
CREATE TABLE `odm_request_order_log` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `creator` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '创建人',
  `is_valid` tinyint(1) DEFAULT '1' COMMENT '是否有效 (1-有效, 0-无效)',
  `origin_id` bigint(20) NOT NULL COMMENT '操作主表的主键ID (关联 odm_request_order.id)',
  `related_work_order_no` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '关联工单号',
  `operator_user_id` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '操作用户ID',
  `operation_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  `operation_desc` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '操作描述',
  `changed_field_name` varchar(200) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更字段名',
  `changed_value` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '变更后值',
  PRIMARY KEY (`id`),
  KEY `idx_work_order_id` (`origin_id`),
  KEY `idx_related_work_order_no` (`related_work_order_no`),
  KEY `idx_operator_user_id` (`operator_user_id`),
  KEY `idx_operation_time` (`operation_time`),
  KEY `idx_changed_field_name` (`changed_field_name`),
  KEY `idx_operation_desc` (`operation_desc`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='ODM需求工单操作记录表'
```

